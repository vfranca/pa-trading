[tool.poetry]
name = "pa-tk"
version = "0.1.0"
description = "price action - tool kit de comandos"
authors = ["Valmir Franca <vfranca3@gmail.com>"]
readme = "README.md"
packages = [{include = "pa_tk"}]

[tool.poetry.dependencies]
python = "^3.9"
click = "^8.1.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
pa = "pa_tk.pa:cli"
